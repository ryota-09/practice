const math = require('../math');
const assert = require('power-assert');

describe('',() => {
  describe('',() => {
    it('_countプロパティの初期値はゼロである', () => {
      assert.equal(math._count(0),0);
    });
    it('_countプロパティの初期値はゼロである', () => {
      assert.equal(math._count(0),0);
    });
  });
  describe('',() => {

  });
});
